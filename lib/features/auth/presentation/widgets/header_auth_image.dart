import 'package:flutter/material.dart';

class HeaderAuthImage extends StatelessWidget {
  const HeaderAuthImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('images/logo/doctor-app-logo.png', height: 140, width: 150);
  }
}
